# Proxy API requests to backend (MUST come before SPA fallback)
# 200! forces proxy even when a local file exists
/api/*  https://mynotesapp.runasp.net/api/:splat  200!

# SPA routing (catch-all for Angular routes)
/*    /index.html   200
